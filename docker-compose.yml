services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: omni-spatial-ai-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - '${APP_PORT:-3002}:3000'

